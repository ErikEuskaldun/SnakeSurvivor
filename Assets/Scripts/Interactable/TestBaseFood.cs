using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestBaseFood : MonoBehaviour
{
    float spawnTime;
    float despawnTime;
    int pointsGiven;
    float tailLenghtGiven; //if 0.5 only give 1 each 2
    bool canDespawn;

    //Change stats each level (5 levels?)
    //Start whit 1 food unlocked
    //Debuf if is not taken
}
